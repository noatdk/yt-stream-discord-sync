(function() {
    "use strict";

    const constants = Object.freeze({
        ENABLED_KEY: "discordEnabled",
        JUMP_OFFSET_KEY: "jumpOffsetSeconds",
        DEFAULT_JUMP_OFFSET_SECONDS: 25
    });

    globalThis.YT_DISCORD_SYNC_CONSTANTS = constants;
})();
